# Copy this file to ".env" and fill in the values.
# ".env" is git-ignored and must never be committed.

# Base URL of the application under test.
BASE_URL=https://dev.admin.avtoikonom.com

# Login credentials for the test account.
USER_EMAIL=your_email_here
USER_PASSWORD=your_password_here

# Optional. API base URL. Defaults to BASE_URL with admin. replaced by api.
# API_BASE_URL=https://dev.api.avtoikonom.com

# Optional. Log verbosity: error | warn | info | debug. Defaults to "info".
LOG_LEVEL=info
